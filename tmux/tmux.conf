##### ===============================
##### 0. ENVIRONMENT & TERMINAL SETUP
##### ===============================

# Set terminal with truecolor support
set -g default-terminal "tmux-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

# Set terminal to run with zsh
set-option -g default-shell /bin/zsh

# Extend scrollback history
set -g history-limit 10000

# Enable mouse support
set-option -g mouse on

# Faster escape timing for better responsiveness
set-option -g escape-time 0

# Prevent auto-renaming window titles
set-option -g allow-rename off

# Vi-style keybinds for easier movement
set-option -g status-keys vi
setw -g mode-keys vi

# Start window and pane numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Move status bar to the top
set-option -g status-position top


##### ====================
##### 1. PREFIX & KEYBINDINGS
##### ====================

# Change prefix from Ctrl-b to Ctrl-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Quick reload of tmux config
bind r source-file ~/.dotfiles/tmux/.tmux.conf \; display-message "Config reloaded 🔁"

# Split panes shortcuts
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Resize panes using Ctrl + hjkl
bind -r C-h resize-pane -L 5
bind -r C-j resize-pane -D 5
bind -r C-k resize-pane -U 5
bind -r C-l resize-pane -R 5

# Move between panes like Vim
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

##### =====================
##### 2. STATUS BAR: POWERLINE STYLE (DEEP PURPLE + NEON GREEN)
##### =====================

# Enable status bar, update every 2s
set -g status on
set -g status-interval 2
set -g status-justify left

# Global styling: Deep purple background, soft white text
set -g status-style bg=#1a1b26,fg=#c0caf5
set -g message-style bg=#1a1b26,fg=#a6e3a1

# --- Left Section: Session Info ---
# Segment 1: Green segment for session name
# Segment 2: Purple arrow separating it
set -g status-left-length 60
set -g status-left "#[bg=#a6e3a1,fg=#1a1b26] 🧠 #S #[bg=#1a1b26,fg=#a6e3a1]"

# --- Window List Styling ---
# Current/Inactive windows: purple background, green accents
set -g window-status-format "#[bg=#1a1b26,fg=#a6e3a1] #I:#W #[fg=#1a1b26,bg=#1a1b26]"
set -g window-status-current-format "#[bg=#1a1b26,fg=#a6e3a1,bold] #I:#W #[fg=#1a1b26,bg=#1a1b26]"

# --- Right Section: Clock, Date, Uptime ---
# Clock: blue accent
# Date: green
# Uptime: pink accent to finish it off
set -g status-right-length 100
set -g status-right "#[fg=#7aa2f7,bg=#1a1b26]#[bg=#7aa2f7,fg=#1a1b26] 🕒 %H:%M \
#[fg=#a6e3a1,bg=#7aa2f7]#[bg=#a6e3a1,fg=#1a1b26] 📅 %Y-%m-%d \
#[fg=#f5c2e7,bg=#a6e3a1]#[bg=#f5c2e7,fg=#1a1b26] ⏱ #(uptime -p)"

##### ======================
##### 3. COPY MODE ENHANCEMENTS
##### ======================

# Vi-style copy mode for selection
bind-key -T copy-mode-vi v send -X begin-selection
bind-key -T copy-mode-vi y send -X copy-selection
bind-key -T copy-mode-vi Escape send -X cancel


##### ========================
##### 4. PLUGINS (TPM + OTHERS)
##### ========================

# Plugin manager (using custom plugin directory)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Load TPM from custom path
run '~/.dotfiles/tmux/plugins/tpm/tpm'
